/**
 * Параметры размеров цилиндрического поля.
 * width — число секторов по окружности, height — число уровней по вертикали.
 */
export interface BoardDimensions {
  width: number;
  height: number;
}
