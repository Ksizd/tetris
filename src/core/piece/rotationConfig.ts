/**
 * Выбор системы вращения. На старте используем простое вращение без wall-kick.
 */
export type RotationMode = 'simple' | 'wall-kick';

export const CURRENT_ROTATION_MODE: RotationMode = 'simple';
